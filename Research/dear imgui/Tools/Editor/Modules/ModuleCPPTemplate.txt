/******************************************************************************/
/*
  File:    XXXXXXModule.cpp
  Author:  Alejandro Hitti
  Date:    11/19/14
  Brief:   The XXXXXX Module handles the inspection of all the components
           inside of the selected object.

  All content © 2014-2015 DigiPen (USA) Corporation, all rights reserved.
*/
/******************************************************************************/

// Includes
//------------------------------------------------------------------------------
#include "stdinc.h"

#include "XXXXXXModule.h"

namespace Editor
{
  bool XXXXXXModule::m_IsActive = false;
  bool XXXXXXModule::m_IsVisible = true;

  // Initializes the Module
  bool XXXXXXModule::XXXXXXModuleInitialize()
  {
    return true;
  }

  // Checks if the module is currently active
  bool XXXXXXModule::XXXXXXModuleIsActive()
  {
    return m_IsActive;
  }

  // Sets the module active flag to the state passed in
  void XXXXXXModule::XXXXXXModuleSetActive(bool state)
  {
    m_IsActive = state;
  }

  // Checks if the module is currently visible
  bool XXXXXXModule::XXXXXXModuleIsVisible()
  {
    return m_IsVisible;
  }

  // Sets the module visible flag to the state passed in
  void XXXXXXModule::XXXXXXModuleSetVisible(bool state)
  {
    m_IsVisible = state;
  }

  // Updates the module
  void XXXXXXModule::XXXXXXModuleUpdate()
  {
    if (!m_IsVisible || !m_IsActive)
      return;

    ImGui::Begin("XXXXXX", &m_IsActive);
    ImGui::Text("I'm the XXXXXX Module");
    ImGui::ShowStyleEditor();
    ImGui::End();
  }

}
